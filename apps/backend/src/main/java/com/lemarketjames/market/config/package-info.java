/**
 * Configuration for the market feature: the {@code sim.*} properties, the UTC clock bean, and
 * scheduling, which is enabled only when {@code sim.enabled} is true so tests have no background
 * threads moving prices.
 */
package com.lemarketjames.market.config;
