# common

Shared code used across feature packages (e.g. `auth`, future `orders`, `accounts`): global exception handlers, common response/error DTOs, base utilities. Nothing feature-specific belongs here.
