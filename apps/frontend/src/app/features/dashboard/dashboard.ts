import { Component } from '@angular/core';

@Component({
  selector: 'app-dashboard',
  standalone: true,
  templateUrl: './dashboard.html',
  styleUrl: './dashboard.css',
})
/**
 * Dashboard Component
 *
 * Placeholder landing page after a successful login. The full dashboard from the
 * LeUI mockup (sidebar, portfolio stats, holdings) will replace this later.
 */
export class Dashboard {}
