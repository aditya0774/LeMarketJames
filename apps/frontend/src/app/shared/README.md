# shared

Reusable, presentational building blocks used across multiple features: dumb/presentational components, pipes, directives, and shared models/interfaces. Nothing here should depend on a specific feature or hold app-wide singleton state (that belongs in `core/`).
